import { Component } from '@angular/core';

@Component({ selector: 'app-returns', standalone: true, template: `<div><h1>Declaraciones</h1><p>Gestiona las declaraciones de impuestos.</p></div>` })
export class ReturnsComponent { }
